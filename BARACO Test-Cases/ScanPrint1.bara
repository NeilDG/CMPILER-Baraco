public class ScanPrint1:

	void main():
		string name = "";
		int age = 0;
		string thesisTitle = "";

		scan("Enter your name: ", name);
		scan("Enter your age: ", age);

		println("You have been identified as " + name + " with the age of " + age + "!");

		println("Answer this security question!");
		scan("What's the title of your thesis?", thesisTitle);

		println("You entered " + thesisTitle +  "! Access granted!");

	end

end
